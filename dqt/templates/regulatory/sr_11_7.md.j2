# Model Monitoring Report — SR 11-7 (Federal Reserve)

**Model:** {{ run.target_col or "(unspecified)" }}
**Reporting period:** {{ run.created_at }}
**Workspace:** {{ run.workspace or "default" }}
**Run id:** #{{ run.id }}

## 1. Executive summary

| Metric | Value |
|---|---|
| Records analysed | {{ "{:,}".format(run.n_rows or 0) }} |
| Features monitored | {{ run.n_features or 0 }} |
| Features at DRIFT (red) | {{ run.red or 0 }} |
| Features at WATCH (yellow) | {{ run.yellow or 0 }} |
| Features at STABLE (green) | {{ run.green or 0 }} |

{% if (run.red or 0) > 0 %}
> **Action required:** {{ run.red }} feature(s) crossed the DRIFT threshold
> and must be reviewed by Model Risk Management within 30 days per
> internal policy aligned with SR 11-7 §V.A (ongoing monitoring).
{% else %}
> **Conclusion:** No feature breached the DRIFT threshold during this
> reporting period. Model continues to operate within the bounds of
> the validation envelope.
{% endif %}

## 2. Population stability

| Feature | PSI peak | Severity | Verdict |
|---|---|---|---|
{% for f in offenders -%}
| {{ f.feature }} | {{ "%.2f"|format((f.summary or {}).get("psi_max") or 0) }} | {{ f.severity|upper }} | {{ f.verdict }} |
{% endfor %}

PSI thresholds applied: 0.10 (WATCH), 0.25 (DRIFT) — banking convention,
configurable via ``DQT_THRESHOLDS_PATH`` for institution-specific
calibration.

## 3. Bin stability (pairwise z-score)

For binary targets, every bin pair is compared via a two-proportion z
test; values close to 1 indicate well-separated bins. A drop below
0.6 in the worst period flags a feature as DRIFT.

## 4. Performance metrics (when ground truth available)

{% if performance %}
| Period | n | AUC | Gini | KS |
|---|---|---|---|---|
{% for p in performance -%}
| {{ p.period or "(global)" }} | {{ "{:,}".format(p.n or 0) }} | {{ "%.3f"|format(p.auc or 0) }} | {{ "%.3f"|format(p.gini or 0) }} | {{ "%.3f"|format(p.ks or 0) }} |
{% endfor %}
{% else %}
> Ground truth labels were not yet available at the time of this report.
> Performance metrics will be retrofitted via ``dqt labels add`` once
> labels arrive (typically 6 months for credit / 30-90 days for fraud).
{% endif %}

## 5. Reproducibility

* Source: ``{{ run.source or "(in-memory)" }}``
* Time column: ``{{ run.time_col }}``
* Target kind: ``{{ run.target_kind }}``
* Granularity: ``{{ (run.meta or {}).get("granularity") or "auto" }}``

The full feature-by-feature evidence is attached as ``dqt-report.html``.
The hash chain in the audit log (``DQT_AUDIT_HMAC_KEY``) provides a
tamper-evident trail of every monitoring event since model deployment.

---

*Generated by DQT v1.2 — Data Quality Tool. This template is provided
as a starting point; institution-specific MRM committees should adapt
sections 1–5 to internal policy.*
